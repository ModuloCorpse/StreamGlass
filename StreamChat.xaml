<UserControl x:Class="StreamGlass.StreamChat"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="466"
             d:DesignWidth="370"
             SizeChanged="UserControl_SizeChanged">
    <Grid Name="ChatPanelBackground">
        <Grid.RowDefinitions>
            <RowDefinition Height="36"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,5,0,5" Name="ChatPanelHeader" Background="#FF18181B">
            <Button Click="Button_Click">Settings</Button>
        </StackPanel>
        <ScrollViewer Grid.Row="1" Name="ChatScrollViewer" ScrollChanged="ChatScrollViewer_ScrollChanged">
            <DockPanel Name="ChatPanelDock" LastChildFill="False">
                <StackPanel Name="ChatPanel" DockPanel.Dock="Bottom" VerticalAlignment="Top" Background="#FF18181B" />
            </DockPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
