<Window xmlns:StreamChat="clr-namespace:StreamGlass.StreamChat"  x:Class="StreamGlass.StreamGlassWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:streamglass="clr-namespace:StreamGlass"
        mc:Ignorable="d"
        Title="StreamGlass"
        MinHeight="500"
        MinWidth="800"
        Icon="/Assets/stream-glass-logo.ico">
    <DockPanel LastChildFill="True">
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_File" />
            <MenuItem Header="_Settings" Click="SettingsToolStripMenuItem_Click"/>
            <MenuItem Header="_Help">
                <MenuItem Header="_Logs"  Click="LogsToolStripMenuItem_Click" />
                <MenuItem Header="_About" />
            </MenuItem>
        </Menu>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2.2*"></ColumnDefinition>
                <ColumnDefinition Width="2.8*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <StreamChat:Chat x:Name="StreamChatPanel" Grid.Column="0"/>
            <StackPanel Grid.Column="1">
                <ComboBox Margin="5,5,0,0" Name="CommandProfilesComboBox" Height="26" Width="120" SelectionChanged="CommandProfilesComboBox_SelectionChanged"/>
            </StackPanel>
        </Grid>
    </DockPanel>
</Window>
