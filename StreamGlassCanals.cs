namespace StreamGlass
{
    enum StreamGlassCanals
    {
        CHAT_MESSAGE,
        CHAT_CONNECTED,
        CHAT_JOINED,
        USER_JOINED,
        UPDATE_STREAM_INFO,
        STREAM_START,
        STREAM_STOP,
        DONATION,
        FOLLOW,
        RAID,
        REWARD,
        COMMANDS,
        PROFILE_CHANGED_MENU_ITEM,
        BAN,
        HELD_MESSAGE,
        ALLOW_MESSAGE,
        HELD_MESSAGE_MODERATED,
        CHAT_CLEAR,
        CHAT_CLEAR_USER,
        CHAT_CLEAR_MESSAGE
    }
}